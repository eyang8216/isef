# Archive manifest

Created 2026-09-10. This archive preserves materials removed from the active working set during folder preparation. No scientific files were deleted.

`root-history/` contains superseded root-level plans, earlier professor/lab documents, the legacy solver copy and cleanup scripts.

`generated/` contains transient locks, LaTeX scratch files, caches, bytecode directories and the superseded paper PDF.

`legacy/` is reserved for later provenance moves.

The pre-existing `archive/` tree remains untouched and contains earlier project history.
